{% extends 'clean.html.twig' %}

{% block title %}{{post.title}}{% endblock %}
{# {% block bg %}'{{post.image}}'{% endblock %} #}

{% block header %}

	{% endblock %}

{% block body %}
	<!-- Post Content-->
	<div class="container text-center">
	<h2> {{ post.title }} </h2><br>
	<article class="mb-4">
		<div class="container px-4 px-lg-5">
			<div class="row gx-4 gx-lg-5 justify-content-center">
				<div class="col-md-10 col-lg-8 col-xl-7">
					{# {{ dump(post) }} #}
					{{ post.content }}
					<br><br>
				</div>
			</div>
		</div>
	</article>
    </div>
	<hr>
	<article class="mb-4">
		<div class="container px-4 px-lg-5">
			<div class="row gx-4 gx-lg-5 justify-content-center">
				<div class="col-md-10 col-lg-8 col-xl-7">
					{# {{ dump(post) }} #}
					<img src="{{ post.image }}" alt="" style="width: 100%">
					<br><br>
					 {{ post.content }}
					<br><br>
				</div>
				
				<p class="post-meta col-md-10 col-lg-8 col-xl-7" style="color: hsl(0, 0%, 100%)">							 
					Article posté par  <a href="#!" style="color: hsl(0, 0%, 100%)"><I>"{{post.user.pseudo}}"</a></I>  le  {{post.createdAt | date("d/m/Y")}}
				</p>
			</div>
		</div>
	</article>
{% endblock %}
