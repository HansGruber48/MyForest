<!-- Navigation--> 
<nav class="navbar navbar-expand-lg">
    <div class="container px-4 px-lg-5">
        <!--<a class="navbar-brand" href="{{ path('home') }}">Start Bootstrap</a>-->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">
                <li class="nav-item" style="color:white">
                    <a class="nav-link" href="{{ path('home') }}" style="color: hsl(0, 0%, 100%)">Accueil</a>
                </li>
                <li class="nav-item" style="color:white">
                    <a class="nav-link" href="{{ path('post_home') }}" style="color: hsl(0, 0%, 100%)">Articles</a>
                </li>
                {% if not is_granted("ROLE_USER") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}" style="color: hsl(0, 0%, 100%)">Enregistrement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}" style="color: hsl(0, 0%, 100%)">Connexion</a>
                </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('formulaire') }}" style="color: hsl(0, 0%, 100%)">Poster un article</a>
            </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" style="color: hsl(0, 0%, 100%)">Déconnexion</a>
                </li>
            {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('page_contact') }}" style="color: hsl(0, 0%, 100%)">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('page_about') }}" style="color: hsl(0, 0%, 100%)">A propos de</a>
                </li>
                
                {# <li class="nav-item">
                    <a class="nav-link" href="{{ path('post_view',{slug:post.}) }}">Articles</a>
                </li> #}

                


               

            </ul>
        </div>
    </div>
</nav>